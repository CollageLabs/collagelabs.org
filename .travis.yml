sudo: required
services: docker
dist: bionic
group: edge
language: minimal
branches:
    only:
        - develop
        - /^snyk.*$/
        - /^dependabot.*$/
cache:
    directories:
        - "$HOME/.cache/yarn"
        - node_modules
        - vendor
before_script:
    - curl -sLO https://github.com/lando/lando/releases/download/v3.0.0-rrc.1/lando-v3.0.0-rrc.1.deb && sudo dpkg -i lando-v3.0.0-rrc.1.deb
    - rm lando-v3.0.0-rrc.1.deb
script:
    - lando start
    - lando yarn run build:pwa
